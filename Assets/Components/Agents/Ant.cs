using UnityEngine;

public class Ant : MonoBehaviour
{

    public float block_x, block_y, block_z;
    public float health;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
